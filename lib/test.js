const { readTextToArray, parseTextData, randomParseTextToArray } = require("./helper");
const { random } = require("./until");

const test = () => {


  console.log(randomParseTextToArray("./helper/gg-alert.txt"));
};
test();
